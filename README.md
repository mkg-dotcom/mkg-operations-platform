# MKG Operations Platform

Responsive React operations dashboard for MKG Business Process Outsourcing Services.

## Local development

```bash
pnpm install
pnpm dev
```

## Deployment

Import the GitHub repository in Vercel. Vercel detects Vite automatically; the build command is `vite build` and the output directory is `dist`.

